# Projects

Multi-skill composite workflows and custom automation projects.

Each project can have its own `schedule/` directory for recurring execution.

```
projects/<project-name>/
├── schedule/
│   ├── install.sh
│   ├── run.sh
│   └── logs/
└── ...
```
